# frozen_string_literal: true

source 'https://rubygems.org'

gem 'metasploit-framework', '~> 6.0'

group :development, :test do
  gem 'rubocop', require: false
  gem 'rspec', require: false 
  # Add other development or testing gems as needed
end
